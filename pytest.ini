[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
    --color=yes
filterwarnings =
    ignore::DeprecationWarning:pymysql.*
markers =
    slow: 실행이 오래 걸리는 테스트
